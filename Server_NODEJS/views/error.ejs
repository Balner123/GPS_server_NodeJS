<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/_head.ejs', {title: 'Error'}) %>
</head>
<body>
    <%- include('partials/_navbar.ejs') %>

    <div class="content-wrapper">
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-8 text-center">
                    <div class="card border-danger shadow-lg">
                        <div class="card-header bg-danger text-white">
                            <h3 class="mb-0"><i class="fas fa-exclamation-triangle me-2"></i>Something went wrong</h3>
                        </div>
                        <div class="card-body py-5">
                            <h4 class="card-title text-danger mb-4">An unexpected error occurred</h4>
                            
                            <% if (typeof message !== 'undefined' && message) { %>
                                <p class="card-text text-muted mb-4"><%= message %></p>
                            <% } else { %>
                                <p class="card-text text-muted mb-4">We are experiencing some technical issues. Please try again later.</p>
                            <% } %>

                            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                                <a href="/" class="btn btn-primary btn-lg px-4 gap-3">
                                    <i class="fas fa-home me-2"></i>Return Home
                                </a>
                                <a href="javascript:history.back()" class="btn btn-outline-secondary btn-lg px-4">
                                    <i class="fas fa-arrow-left me-2"></i>Go Back
                                </a>
                            </div>
                        </div>
                        <div class="card-footer text-muted">
                            If this problem persists, please contact support.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/_footer.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/common.js"></script>
</body>
</html>
