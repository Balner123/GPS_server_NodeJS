<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">GPS Tracking System</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentPage !== 'undefined' && currentPage === 'index') ? 'active' : '' %>" href="/">Přehled</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentPage !== 'undefined' && currentPage === 'device') ? 'active' : '' %>" href="/device">Správa zařízení</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <% if (currentUser) { %>
                    <li class="nav-item">
                        <span class="navbar-text me-3">
                            Přihlášen: <%= currentUser.username %>
                        </span>
                    </li>
                    <% if (currentUser.username === 'root') { %>
                        <li class="nav-item">
                            <a class="nav-link <%= (typeof currentPage !== 'undefined' && currentPage === 'admin-device-settings') ? 'active' : '' %>" href="/admin/device-settings">Spravovat odmlky</a>
                        </li> 
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Odhlásit se</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link <%= (typeof currentPage !== 'undefined' && currentPage === 'login') ? 'active' : '' %>" href="/login">Přihlásit se</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>