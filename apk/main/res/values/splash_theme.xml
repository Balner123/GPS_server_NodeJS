<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme for the splash screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Background used while the splash screen is visible -->
        <item name="windowSplashScreenBackground">@drawable/splash_background</item>

        <!-- Theme to apply after the splash screen finishes -->
        <item name="postSplashScreenTheme">@style/Theme.GpsReporterApp</item>
    </style>
</resources> 